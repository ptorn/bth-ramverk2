import React from "react";
import Layout from "../Layout";
import Chat from "../chat/Connect";

const ChatPage = () => {
    return (
        <Layout>
            <Chat />
        </Layout>
    );
};

export default ChatPage;
